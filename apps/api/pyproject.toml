[project]
name = "proof-of-work-resume-api"
version = "1.0.0"
requires-python = ">=3.12"
description = "Strict validation, deterministic coverage and source-preserving resume export."

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
addopts = "-ra"
